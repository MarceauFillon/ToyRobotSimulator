<%
=begin%>
  Forms that will not reload the page on submit but be considered as an AJAX request.
  When reloaded, the view will use the robot id in the requet parameters.
  Each of the commands and input is disabled if no robot has been created and if the robot has not been placed.
<%
=end%>
<div class="container-fluid">
    <div class="row">
        <div class="col-4">
            <%= form_with scope: :robot, url: robots_rotate_path({direction: "left"}), html:{class:"rotate-form"} do |form| %>
                <% if @robot %>
                    <%= hidden_field_tag("id", @robot.id, :id => "robot_id") %>
                <% end %>

                <p class="text-center">
                <%= form.submit "Left", :disabled => !(!@robot.blank? && @robot.on_table), :class => "btn btn-outline-dark" %>
                </p>
            <% end %>
        </div>

        <div class="col-4">
            <%= form_with scope: :robot, url: robots_move_path do |form| %>
                <% if @robot %>
                    <%= hidden_field_tag("id", @robot.id, :id => "robot_id") %>
                <% end %>
                
                <p class="text-center">
                    <%= form.submit "Move" , :disabled => !(!@robot.blank? && @robot.on_table), :class => "btn btn-outline-dark" %>
                </p>
            <% end %>
        </div>

        <div class="col-4">
            <%= form_with scope: :robot, url: robots_rotate_path({direction: "right"}), html:{class:"rotate-form"} do |form| %>
                <% if @robot %>
                    <%= hidden_field_tag("id", @robot.id, :id => "robot_id") %>
                <% end %>

                <p class="text-center">
                <%= form.submit "Right", :disabled => !(!@robot.blank? && @robot.on_table), :class => "btn btn-outline-dark" %>
                </p>

            <% end %>
        </div>
    </div>
</div>
